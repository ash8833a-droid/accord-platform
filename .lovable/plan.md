## الهدف
إزالة بطاقة "إدارة المالية" (الشريط البارز) نهائياً من صفحة مركز المهام `/admin/tasks`. ستبقى صفحة `/finance-management` المستقلة كما هي، والوصول إليها عبر القائمة الجانبية فقط.

## التغييرات في `src/components/admin/TaskCenterPage.tsx`

1. **حذف بلوك الشريط (السطور 183-212)** — كامل عنصر `<Link to="/finance-management">...</Link>` مع التعليق `{/* Finance management shortcut */}`.

2. **إزالة منطق العدّاد المرتبط بطلبات الصرف (لم يعد مستخدماً):**
   - حذف `useState` الخاص بـ `pendingPayments` (السطر 77).
   - حذف `useEffect` الذي يستعلم من جدول `payment_requests` ويشترك في realtime (يشمل `fetchCount`، `.from("payment_requests")`، والاشتراك على الـ channel).

3. **تنظيف الاستيرادات غير المستخدمة:**
   - من `lucide-react`: إزالة `Banknote` و `ArrowLeft` (إن لم يُستخدما في مكان آخر بالملف).
   - الإبقاء على `Link` لأنه مستخدم في السطر 486.

## ما لن يتغيّر
- صفحة `/finance-management` ومحتواها.
- رابط "إدارة المالية" في القائمة الجانبية (`AppShell.tsx`) و `pages.ts`.
- صلاحيات الوصول للصفحة.

## التحقق بعد التنفيذ
- فتح `/admin/tasks` والتأكد من اختفاء الشريط تماماً دون ترك فراغ غير مرغوب.
- التأكد من أن `/finance-management` لا تزال تعمل عبر القائمة الجانبية.
- عدم وجود تحذيرات TypeScript عن استيرادات/متغيرات غير مستخدمة.